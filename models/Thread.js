/// text - String
/// delete_password - String?
/// created_on(date&time)
/// bumped_on(date&time, starts same as created_on)
/// reported(boolean),
/// replies(array) <- reference some other model?
//// Store board title???????

/// Recomend res.redirect to board page /b/{board}